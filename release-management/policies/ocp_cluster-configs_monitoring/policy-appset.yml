---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ocp_cluster-configs_monitoring
  namespace: openshift-gitops
spec:
  generators:
  - matrix:
      generators:
        - list:
            elements:
              - policy: ocp_cluster-configs_monitoring
                revision: main
        - list:
            elements:
            - cluster: ocpac

            - cluster: ocpbd

            - cluster: ocpce

            - cluster: ocphbprd
